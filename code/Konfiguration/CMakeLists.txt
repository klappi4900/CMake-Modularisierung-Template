# Reiner Vertrag (nur Header): die effektiven Einstellungen als Wert-Objekt.
# Kein eigener Quellcode -> nur die api/-Sublib Konfiguration_API, gegen die
# app UND compositor linken. Kein add_module(), da es keine src/ gibt.
add_subdirectory(api)
